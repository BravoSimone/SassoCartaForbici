<!DOCTYPE html>
<html>
<head>
  <title>Sasso, Carta, Forbici</title>
  <%= csrf_meta_tags %>
  <link href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css" rel="stylesheet">
  <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js"></script>
</head>
<body>
<div class="container">
<div class="row">
  <% if current_user.present? %>
    <div class="col-md-12"> <span class="pull-right"> Sei connesso come <%= current_user.email %> |
    <%= link_to('Logout', destroy_user_session_path, :method => :get) %></span></div>
  <% end %>
</div>
  <%= yield %>
</div>

</body>
</html>